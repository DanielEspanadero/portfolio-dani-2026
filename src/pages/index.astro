---
export const prerender = true;

import BaseLayout from '@layouts/BaseLayout.astro';
import Contact from '@components/home/Contact.astro';
import Button from '@components/ui/Button.astro';
import { profile } from '@data/profile';
import { technicalPractices, technologies } from '@data/skills';

const techColors: Record<string, string> = {
  'Java': '#f89820',
  'Spring Boot': '#6db33f',
  'Spring Security': '#5ba033',
  'MySQL': '#4479a1',
  'Docker': '#2496ed',
  'AWS': '#ff9900',
  'Linux': '#fcc624',
  'Angular': '#dd0031',
};
import { experiences } from '@data/experience';
import { projects } from '@data/projects';

const title = 'Daniel Españadero | Software Engineer Java, Spring Boot e IA';
const description = 'Portfolio profesional de Daniel Españadero, Software Engineer especializado en backend, Java, Spring Boot, arquitectura limpia e IA aplicada.';

const principles = [
  'Backend mantenible antes que soluciones espectaculares pero frágiles.',
  'Arquitectura clara para que los equipos puedan evolucionar software sin miedo.',
  'IA aplicada a problemas reales, no como etiqueta de moda.',
  'Comunicación técnica como parte del trabajo de ingeniería.',
];
---

<BaseLayout {title} {description}>
  <section class="hero container" aria-labelledby="hero-title">
    <div class="hero-statement">
      <p class="eyebrow">Backend · Java · Spring Boot · IA aplicada</p>
      <h1 id="hero-title" class="hero-title" aria-label="Software Engineer">
        <span class="hero-title-line">SOFTWARE</span>
        <span class="hero-title-line">ENGINEER</span>
      </h1>
      <div class="hero-actions">
        <Button href="#contacto">Contactar</Button>
        <Button href="#perfil" variant="secondary">Conocer mi perfil</Button>
      </div>
    </div>

    <aside class="hero-profile surface" aria-label="Perfil de Daniel Españadero">
      <div class="hero-photo-frame">
        <img src="/images/dani.png" alt="Retrato profesional de Daniel Españadero" width="420" height="520" loading="eager" decoding="async" />
      </div>
      <div class="hero-profile-content">
        <div class="hero-profile-heading">
          <p class="hero-profile-label">Perfil</p>
          <span class="hero-status">Backend</span>
        </div>
        <h2>Daniel Españadero</h2>
        <p class="hero-role">Backend Software Engineer</p>
        <ul aria-label="Tecnologías principales">
          <li>Java</li>
          <li>Spring Boot</li>
          <li>Spring Security</li>
          <li>Arquitectura backend</li>
          <li>IA aplicada</li>
        </ul>
        <p class="hero-profile-note">Autor de un libro de programación y creador de contenido técnico.</p>
      </div>
    </aside>
  </section>

  <section id="perfil" class="profile-section container" aria-labelledby="profile-title">
    <div class="profile-shell surface">
      <div class="profile-copy">
        <p class="eyebrow">Perfil</p>
        <h2 id="profile-title" class="section-title">Backend engineer con criterio de arquitectura, producto y comunicación técnica.</h2>
        <p>
          Soy Daniel Españadero, Backend Software Engineer especializado en Java, Spring Boot, arquitectura backend e IA aplicada. Construyo software mantenible para sistemas reales, no demos bonitas.
        </p>
      </div>

      <blockquote class="profile-manifesto">
        <p>Construir backend no va de acumular frameworks. Va de tomar buenas decisiones bajo restricciones reales.</p>
      </blockquote>

      <div class="profile-proof" aria-label="Pruebas profesionales de Daniel Españadero">
        <article>
          <span>01</span>
          <h3>Ingeniería backend</h3>
          <p>Java, Spring Boot, Spring Security, APIs REST y sistemas mantenibles.</p>
        </article>
        <article>
          <span>02</span>
          <h3>Arquitectura aplicada</h3>
          <p>Hexagonal Architecture, testing, diseño de APIs y evolución de software.</p>
        </article>
        <article>
          <span>03</span>
          <h3>Autoridad técnica</h3>
          <p>Autor de un libro de programación y creador de contenido para una comunidad de más de <strong>50.000 desarrolladores</strong>.</p>
        </article>
      </div>
    </div>
  </section>

  <section id="stack" class="stack-section container" aria-labelledby="stack-title">
    <p class="eyebrow">Stack y criterio técnico</p>
    <div class="stack-heading-row">
      <h2 id="stack-title" class="section-title">Herramientas visibles. Criterio invisible. Las dos cosas importan.</h2>
      <p>
        Separar tecnologías de habilidades no es estético: es arquitectura de comunicación. Una cosa es con qué construyo; otra, cómo tomo decisiones.
      </p>
    </div>

    <div class="technology-panel surface" aria-labelledby="technologies-title">
      <div class="stack-subheading">
        <span>01</span>
        <h3 id="technologies-title">Tecnologías principales</h3>
        <p>Stack curado para backend Java/Spring, soporte enterprise y entrega de software real.</p>
      </div>
      <div class="technology-grid">
        {technologies.map((technology) => (
          <article class="technology-card" style={`--tc: ${techColors[technology.name] ?? '#34d399'}`}>
            <img src={technology.icon} alt="" width="64" height="64" loading="lazy" decoding="async" />
            <strong>{technology.name}</strong>
            <span>{technology.category}</span>
          </article>
        ))}
      </div>
    </div>

    <div class="practice-panel" aria-labelledby="practices-title">
      <div class="stack-subheading">
        <span>02</span>
        <h3 id="practices-title">Habilidades técnicas</h3>
        <p>La parte que no cabe en un logo: arquitectura, calidad, integración y criterio para mantener sistemas vivos.</p>
      </div>
      <div class="practice-grid">
        {technicalPractices.map((practice, index) => (
          <article class="practice-card surface">
            <span>0{index + 1}</span>
            <h4>{practice.title}</h4>
            <p>{practice.description}</p>
          </article>
        ))}
      </div>
    </div>
  </section>

  <section id="experiencia" class="experience-section container" aria-labelledby="experience-title">
    <div class="section-heading-row">
      <div>
        <p class="eyebrow">Experiencia</p>
        <h2 id="experience-title" class="section-title">Software en entornos reales, no solo demos bonitas.</h2>
      </div>
      <p>
        Experiencia profesional orientada a desarrollo backend, integración de sistemas y construcción de soluciones mantenibles en contexto empresarial.
      </p>
    </div>
    <div class="experience-list">
      {experiences.map((experience) => (
        <article class="experience-item">
          <div>
            <span>{experience.period}</span>
            <h3>{experience.company}</h3>
            <p>{experience.role}</p>
          </div>
          <div>
            <p>{experience.description}</p>
            <ul>
              {experience.highlights.map((highlight) => <li>{highlight}</li>)}
            </ul>
          </div>
        </article>
      ))}
    </div>
  </section>

  <section class="principles-section container" aria-labelledby="principles-title">
    <p class="eyebrow">Principios</p>
    <h2 id="principles-title" class="section-title">La técnica importa, pero el criterio decide.</h2>
    <div class="principles-grid">
      {principles.map((principle, index) => (
        <article class="principle-card">
          <span>0{index + 1}</span>
          <p>{principle}</p>
        </article>
      ))}
    </div>
  </section>

  <section id="autoridad" class="authority-section container" aria-labelledby="authority-title">
    <div class="authority-card surface">
      <div>
        <p class="eyebrow">Autoridad técnica</p>
        <h2 id="authority-title" class="section-title">Autor, creador y comunicador técnico.</h2>
        <p>
          Su libro y su canal de YouTube refuerzan algo que en ingeniería de software es diferencial: la capacidad de convertir conocimiento técnico en explicaciones claras, accionables y útiles para otros desarrolladores.
        </p>
      </div>
      <div class="authority-actions">
        <Button href={profile.links.book}>Ver libro</Button>
        <Button href={profile.links.youtube} variant="secondary">Ver YouTube</Button>
      </div>
    </div>
  </section>

  <section class="projects-section container" aria-labelledby="projects-title">
    <p class="eyebrow">Contenido técnico</p>
    <h2 id="projects-title" class="section-title">Ideas técnicas convertidas en contenido práctico.</h2>
    <div class="project-row">
      {projects.map((project) => (
        <article class="project-card surface">
          <h3>{project.title}</h3>
          <p>{project.description}</p>
          <a href={project.href} target="_blank" rel="noreferrer">Ver recurso</a>
        </article>
      ))}
    </div>
  </section>

  <Contact />
</BaseLayout>

<style>
  .hero {
    position: relative;
    isolation: isolate;
    display: grid;
    grid-template-columns: minmax(0, 1.28fr) minmax(300px, 0.72fr);
    gap: clamp(2.5rem, 5vw, 4.25rem);
    align-items: center;
    min-height: calc(86svh - 72px);
    padding-block: clamp(3.5rem, 8vw, 6.5rem);
  }

  .hero::before,
  .hero::after {
    position: absolute;
    z-index: -1;
    pointer-events: none;
    content: '';
  }

  .hero::before {
    inset: 6% 38% 12% -18%;
    background: radial-gradient(circle at 42% 52%, rgba(52, 211, 153, 0.18), transparent 58%);
    filter: blur(12px);
  }

  .hero::after {
    inset: 14% 42% 18% -8%;
    background-image: radial-gradient(circle, rgba(52, 211, 153, 0.15) 1px, transparent 1.4px);
    background-size: 28px 28px;
    mask-image: linear-gradient(90deg, transparent, black 18%, black 62%, transparent),
      linear-gradient(180deg, transparent, black 16%, black 78%, transparent);
    mask-composite: intersect;
    opacity: 0.48;
  }

  .hero-statement,
  .hero-profile {
    position: relative;
    z-index: 1;
  }

  .hero-statement {
    min-width: 0;
  }

  .hero-title {
    display: grid;
    gap: 0.03em;
    margin: 0;
    font-size: clamp(4rem, 7.8vw, 7.25rem);
    line-height: 0.88;
    letter-spacing: -0.025em;
    text-transform: uppercase;
    overflow-wrap: normal;
    word-break: keep-all;
    user-select: none;
  }

  .hero-statement .eyebrow {
    user-select: none;
  }

  .hero-title-line {
    display: block;
    white-space: nowrap;
  }

  .hero-profile {
    overflow: hidden;
    box-shadow:
      0 28px 90px rgba(0, 0, 0, 0.34),
      0 0 70px rgba(52, 211, 153, 0.08),
      0 0 90px rgba(167, 243, 208, 0.05);
    background:
      radial-gradient(circle at 18% 16%, rgba(52, 211, 153, 0.2), transparent 14rem),
      linear-gradient(180deg, rgba(255, 255, 255, 0.06), transparent 42%),
      rgba(13, 19, 34, 0.9);
  }

  .hero-photo-frame {
    margin: clamp(0.85rem, 2vw, 1rem) clamp(0.85rem, 2vw, 1rem) 0;
    overflow: hidden;
    border: 1px solid var(--color-border);
    border-radius: calc(var(--radius-lg) - 0.5rem);
    background: rgba(255, 255, 255, 0.035);
  }

  .hero-photo-frame img {
    width: 100%;
    height: auto;
    object-fit: contain;
    object-position: center;
  }

  .hero-profile-content {
    display: grid;
    gap: 0.8rem;
    padding: clamp(1.25rem, 3vw, 1.8rem);
  }

  .hero-profile-heading {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
  }

  .hero-profile-label {
    margin: 0;
    color: var(--color-primary);
    font-family: var(--font-mono);
    font-size: 0.78rem;
    letter-spacing: 0.16em;
    text-transform: uppercase;
  }

  .hero-status {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    padding: 0.35rem 0.6rem;
    border: 1px solid rgba(167, 243, 208, 0.24);
    border-radius: 999px;
    color: var(--color-accent);
    background: rgba(167, 243, 208, 0.08);
    font-size: 0.75rem;
    font-weight: 800;
  }

  .hero-status::before {
    width: 0.42rem;
    height: 0.42rem;
    border-radius: 999px;
    background: currentColor;
    content: '';
  }

  .hero-profile h2 {
    margin: 0;
    font-size: clamp(2.15rem, 4vw, 3.25rem);
    line-height: 0.95;
    letter-spacing: -0.05em;
  }

  .hero-role {
    margin: 0;
    color: var(--color-primary);
    font-family: var(--font-mono);
    font-size: 0.9rem;
    font-weight: 800;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .hero-profile ul {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin: 0.5rem 0 0;
    padding: 0;
    list-style: none;
  }

  .hero-profile li {
    padding: 0.45rem 0.65rem;
    border: 1px solid var(--color-border);
    border-radius: 999px;
    color: var(--color-text);
    background: rgba(255, 255, 255, 0.035);
    font-size: 0.82rem;
    font-weight: 700;
  }

  .hero-profile-note {
    margin: 0.25rem 0 0;
    padding-top: 1rem;
    border-top: 1px solid var(--color-border);
    color: var(--color-text-muted);
    font-size: 0.95rem;
    line-height: 1.55;
  }

  .section-heading-row > p,
  .authority-card p,
  .project-card p {
    color: var(--color-text-muted);
  }

  .hero-actions,
  .authority-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    margin-top: 1.75rem;
  }

  .profile-section,
  .stack-section,
  .experience-section,
  .principles-section,
  .authority-section,
  .projects-section {
    padding-block: var(--section-space);
  }

  .profile-section {
    position: relative;
  }

  .authority-card {
    padding: clamp(1.5rem, 4vw, 3rem);
  }

  .profile-shell {
    position: relative;
    display: grid;
    grid-template-columns: minmax(0, 0.92fr) minmax(0, 1.08fr);
    gap: clamp(2rem, 5vw, 4.5rem);
    overflow: hidden;
    padding: clamp(1.35rem, 4vw, 3.25rem);
    background:
      radial-gradient(circle at 78% 18%, rgba(52, 211, 153, 0.2), transparent 18rem),
      linear-gradient(135deg, rgba(255, 255, 255, 0.055), transparent 28%),
      linear-gradient(145deg, rgba(16, 24, 39, 0.94), rgba(8, 13, 22, 0.82));
  }

  .profile-shell::before,
  .profile-shell::after {
    position: absolute;
    pointer-events: none;
    content: '';
  }

  .profile-shell::before {
    inset: 0;
    background-image: radial-gradient(circle, rgba(167, 243, 208, 0.12) 1px, transparent 1.4px);
    background-size: 26px 26px;
    mask-image: linear-gradient(120deg, transparent, black 22%, transparent 74%);
    opacity: 0.38;
  }

  .profile-shell::after {
    right: clamp(1rem, 3vw, 2rem);
    bottom: clamp(1rem, 3vw, 2rem);
    width: min(42%, 420px);
    height: 1px;
    background: linear-gradient(90deg, transparent, rgba(167, 243, 208, 0.42), transparent);
    box-shadow:
      -72px -82px 0 rgba(167, 243, 208, 0.1),
      -24px -42px 0 rgba(167, 243, 208, 0.16),
      -120px -18px 0 rgba(167, 243, 208, 0.08);
  }

  .profile-copy,
  .profile-manifesto,
  .profile-proof {
    position: relative;
    z-index: 1;
  }

  .profile-copy p:not(.eyebrow) {
    max-width: 620px;
    color: var(--color-text-muted);
    font-size: clamp(1.02rem, 2vw, 1.18rem);
    line-height: 1.75;
  }

  .profile-manifesto {
    align-self: end;
    margin: 0;
    padding-left: clamp(1rem, 3vw, 2rem);
    border-left: 1px solid rgba(167, 243, 208, 0.28);
  }

  .profile-manifesto p {
    margin: 0;
    color: var(--color-text);
    font-size: clamp(1.8rem, 4vw, 3.45rem);
    font-weight: 800;
    line-height: 0.98;
    letter-spacing: -0.06em;
  }

  .profile-proof {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 1px;
    grid-column: 1 / -1;
    overflow: hidden;
    margin-top: clamp(0.5rem, 2vw, 1rem);
    border: 1px solid var(--color-border);
    border-radius: calc(var(--radius-lg) - 0.25rem);
    background:
      linear-gradient(90deg, rgba(52, 211, 153, 0.22), transparent 24%, transparent 76%, rgba(167, 243, 208, 0.12)),
      var(--color-border);
  }

  .profile-proof article {
    min-height: 230px;
    padding: clamp(1.1rem, 2.5vw, 1.55rem);
    background: rgba(5, 7, 13, 0.58);
  }

  .profile-proof span {
    color: var(--color-primary);
    font-family: var(--font-mono);
    font-size: 0.82rem;
    font-weight: 800;
  }

  .profile-proof h3 {
    margin: clamp(2rem, 5vw, 3.6rem) 0 0.75rem;
    font-size: clamp(1.15rem, 2vw, 1.45rem);
    line-height: 1.1;
    letter-spacing: -0.04em;
  }

  .profile-proof p {
    margin: 0;
    color: var(--color-text-muted);
    font-size: 0.98rem;
    line-height: 1.65;
  }

  .authority-card p {
    font-size: 1.12rem;
    line-height: 1.8;
  }

  .profile-proof strong {
    color: var(--color-text);
  }

  .principles-grid,
  .project-row {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 1rem;
    margin-top: 2rem;
  }

  .stack-heading-row {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(280px, 0.58fr);
    gap: clamp(2rem, 5vw, 4rem);
    align-items: end;
  }

  .stack-heading-row > p {
    margin: 0;
    color: var(--color-text-muted);
    font-size: 1.08rem;
    line-height: 1.75;
  }

  .technology-panel {
    position: relative;
    overflow: hidden;
    margin-top: clamp(2rem, 5vw, 3rem);
    padding: clamp(1.25rem, 3vw, 2rem);
    background:
      radial-gradient(circle at 8% 12%, rgba(52, 211, 153, 0.18), transparent 16rem),
      linear-gradient(145deg, rgba(16, 24, 39, 0.94), rgba(8, 13, 22, 0.84));
  }

  .technology-panel::before {
    position: absolute;
    inset: 0;
    pointer-events: none;
    content: '';
    background-image:
      linear-gradient(rgba(167, 243, 208, 0.06) 1px, transparent 1px),
      linear-gradient(90deg, rgba(167, 243, 208, 0.06) 1px, transparent 1px);
    background-size: 48px 48px;
    mask-image: linear-gradient(90deg, black, transparent 78%);
  }

  .stack-subheading,
  .technology-grid,
  .practice-grid {
    position: relative;
    z-index: 1;
  }

  .stack-subheading {
    display: grid;
    grid-template-columns: auto minmax(0, 1fr);
    gap: 0.35rem 1rem;
    max-width: 760px;
  }

  .stack-subheading span {
    grid-row: span 2;
    color: var(--color-primary);
    font-family: var(--font-mono);
    font-size: 0.82rem;
    font-weight: 800;
  }

  .stack-subheading h3 {
    margin: 0;
    font-size: clamp(1.55rem, 3vw, 2.25rem);
    line-height: 1;
    letter-spacing: -0.05em;
  }

  .stack-subheading p {
    margin: 0;
    color: var(--color-text-muted);
    line-height: 1.65;
  }

  .technology-grid {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 1px;
    overflow: hidden;
    margin-top: clamp(1.5rem, 4vw, 2rem);
    border: 1px solid var(--color-border);
    border-radius: calc(var(--radius-lg) - 0.25rem);
    background: var(--color-border);
  }

  .technology-card {
    position: relative;
    display: grid;
    grid-template-rows: auto 1fr auto;
    gap: 0.75rem;
    padding: clamp(1rem, 2.5vw, 1.5rem);
    min-height: 160px;
    background: rgba(5, 7, 13, 0.58);
    transition: background 0.3s ease;
  }

  .technology-card:hover {
    background: color-mix(in srgb, var(--tc) 14%, rgba(5, 7, 13, 0.92));
  }

  .technology-card:hover::before {
    background: linear-gradient(90deg, color-mix(in srgb, var(--tc) 70%, transparent), transparent);
  }

  .technology-card:hover img {
    filter: brightness(0) invert(1) drop-shadow(0 0 10px var(--tc));
  }

  .technology-card:hover strong {
    color: color-mix(in srgb, var(--tc) 80%, var(--color-text));
  }

  .technology-card img {
    width: 3rem;
    height: 3rem;
    filter: brightness(0) invert(1);
  }

  .technology-card::before {
    position: absolute;
    inset: auto auto -1px -1px;
    width: 60%;
    height: 1px;
    background: linear-gradient(90deg, rgba(52, 211, 153, 0.5), transparent);
    content: '';
  }

  .technology-card strong {
    align-self: end;
    font-size: clamp(1.15rem, 2vw, 1.45rem);
    line-height: 1;
    letter-spacing: -0.04em;
  }

  .technology-card span {
    color: var(--color-text-muted);
    font-family: var(--font-mono);
    font-size: 0.78rem;
  }

  .practice-panel {
    display: grid;
    grid-template-columns: minmax(260px, 0.42fr) minmax(0, 1fr);
    gap: clamp(1.5rem, 4vw, 2.5rem);
    margin-top: clamp(2rem, 5vw, 3rem);
    align-items: start;
  }

  .practice-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 1rem;
  }

  .practice-card {
    min-height: 220px;
    padding: clamp(1rem, 2.5vw, 1.35rem);
    background:
      linear-gradient(145deg, rgba(16, 24, 39, 0.9), rgba(8, 13, 22, 0.76));
  }

  .practice-card > span {
    color: var(--color-primary);
    font-family: var(--font-mono);
    font-size: 0.8rem;
    font-weight: 800;
  }

  .practice-card h4 {
    margin: 2.25rem 0 0.75rem;
    font-size: clamp(1.15rem, 2vw, 1.45rem);
    line-height: 1.1;
    letter-spacing: -0.04em;
  }

  .practice-card p {
    margin: 0;
    color: var(--color-text-muted);
    line-height: 1.65;
  }

  .section-heading-row {
    display: grid;
    grid-template-columns: 1fr 0.7fr;
    gap: 2rem;
    align-items: end;
  }

  .section-heading-row > p {
    line-height: 1.75;
    font-size: 1.1rem;
  }

  .experience-list {
    display: grid;
    margin-top: 2rem;
    border-top: 1px solid var(--color-border);
  }

  .experience-item {
    display: grid;
    grid-template-columns: minmax(220px, 320px) 1fr;
    gap: 2rem;
    padding-block: 2rem;
    border-bottom: 1px solid var(--color-border);
  }

  .experience-item span {
    color: var(--color-primary);
    font-family: var(--font-mono);
    font-size: 0.85rem;
  }

  .experience-item h3 {
    margin: 0.5rem 0;
    font-size: clamp(1.45rem, 4vw, 2rem);
    letter-spacing: -0.05em;
  }

  .experience-item p,
  .experience-item li {
    color: var(--color-text-muted);
    line-height: 1.7;
  }

  .principle-card {
    min-height: 220px;
    border: 1px solid var(--color-border);
    border-radius: 1.5rem;
    padding: 1.2rem;
    background: rgba(255, 255, 255, 0.035);
  }

  .principle-card span {
    color: var(--color-accent);
    font-family: var(--font-mono);
  }

  .principle-card p {
    margin-top: 2.5rem;
    font-size: 1.15rem;
    line-height: 1.55;
  }

  .authority-card {
    display: grid;
    grid-template-columns: 1fr auto;
    gap: 2rem;
    align-items: end;
    background:
      linear-gradient(135deg, rgba(52, 211, 153, 0.15), transparent 36%),
      linear-gradient(315deg, rgba(248, 199, 126, 0.12), transparent 42%),
      rgba(13, 19, 34, 0.92);
  }

  .project-row {
    grid-template-columns: repeat(3, minmax(0, 1fr));
  }

  .project-card {
    padding: clamp(1.1rem, 3vw, 1.4rem);
  }

  .project-card h3 {
    margin-top: 0;
    font-size: 1.4rem;
  }

  .project-card a {
    color: var(--color-accent);
    font-weight: 800;
  }

  @media (max-width: 980px) {
    .hero,
    .profile-shell,
    .profile-proof,
    .stack-heading-row,
    .practice-panel,
    .section-heading-row,
    .authority-card,
    .experience-item {
      grid-template-columns: 1fr;
    }

    .technology-grid,
    .principles-grid,
    .project-row {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .hero::before {
      inset: 2% -18% 46% -18%;
    }

    .hero::after {
      inset: 8% -8% 52% -8%;
    }

    .hero {
      align-items: start;
      justify-items: center;
      min-height: auto;
    }

    .hero-statement {
      width: 100%;
      max-width: 760px;
      justify-self: center;
    }

    .hero-title {
      font-size: clamp(4rem, 11.8vw, 6.6rem);
    }

    .hero-profile {
      width: 100%;
      max-width: 520px;
      justify-self: center;
    }

    .profile-section,
    .stack-section,
    .experience-section,
    .principles-section,
    .authority-section,
    .projects-section {
      padding-block: var(--section-space-sm);
    }
  }

  @media (max-width: 620px) {
    .hero {
      gap: 2rem;
      padding-block: 2.75rem 2.75rem;
    }

    .hero-statement,
    .hero-profile {
      max-width: none;
    }

    .hero-title {
      font-size: clamp(2.55rem, 14.8vw, 4.65rem);
      line-height: 0.94;
      letter-spacing: -0.018em;
    }

    .hero-actions,
    .authority-actions {
      display: grid;
      gap: 0.75rem;
      width: 100%;
      margin-top: 1.45rem;
    }

    .hero-profile-content {
      padding: 1.15rem;
    }

    .hero-profile h2 {
      font-size: clamp(1.9rem, 10vw, 2.6rem);
      line-height: 1;
    }

    .hero-profile-heading {
      align-items: flex-start;
      flex-direction: column;
      gap: 0.75rem;
    }

    .profile-shell,
    .authority-card {
      padding: 1.15rem;
    }

    .authority-card p {
      font-size: 1rem;
      line-height: 1.7;
    }

    .profile-manifesto {
      padding-left: 1rem;
    }

    .profile-manifesto p {
      font-size: clamp(1.55rem, 10vw, 2.35rem);
      line-height: 1.02;
    }

    .profile-proof article {
      min-height: auto;
    }

    .section-heading-row > p {
      font-size: 1rem;
      line-height: 1.65;
    }

    .experience-item {
      gap: 1rem;
      padding-block: 1.5rem;
    }

    .experience-item ul {
      padding-left: 1.1rem;
    }

    .principle-card {
      min-height: auto;
      border-radius: var(--radius-md);
    }

    .principle-card p {
      margin-top: 1.5rem;
      font-size: 1.05rem;
    }

    .technology-grid,
    .practice-grid,
    .principles-grid,
    .project-row {
      grid-template-columns: 1fr;
    }

    .technology-card,
    .practice-card {
      min-height: auto;
    }
  }
</style>
