{
  "name": "portfolio-dani-2026",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.1.2",
  "scripts": {
    "preinstall": "node -e \"if (!process.env.npm_execpath?.includes('pnpm')) { console.error('Use pnpm for this project: pnpm install'); process.exit(1); }\"",
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/cloudflare": "latest",
    "@astrojs/sitemap": "latest",
    "astro": "latest",
    "resend": "^6.1.3"
  },
  "devDependencies": {
    "typescript": "latest"
  }
}
